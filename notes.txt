
// https://stackoverflow.com/questions/64792787/easiest-way-to-detect-production-or-dev-environment-in-nextjs
const env = process.env.NODE_ENV
if(env == "development"){
  // do something
}
else if (env == "production"){
 // do something
}

websocket.js:77 WebSocket connection to 'ws://localhost:3000/_next/webpack-hmr' failed: 

Intiial Setup:
1. follow biome instructions
> "biome" is a prettier and eslint replacement

tsrafce - ts react arrow functional component export

# Prisma

npx primsa db push (push to web)
npx prisma generate

Mongo DB Atlas

Be sure to have a .env file with a string and a database to push to
then do:
npx primsa db push
(this will also help with vscode autofill)

mikehail@maildrop.cc
mike hail
mikehail
123321

robboss@maildrop.cc
rob boss
robboss
456654

demo@maildrop.cc
demo
demo

next auth ('current' api) presumes you're on port 3000?

vscode -> check the view toggle ports... (port forwarding issues...)

sudo netstat -nlp

Todo:
1. import cleanup
2. Re-walk through code
3. Creating your own branding
4. Readme documentation
5. Possible extensions
6. About page (on site) ; github repo link
7. Tab header (metadata)

Notes on code:
Custom Hooks are either data stores or SWR calls

This website is created purely for education purposes and in no way intended to be used for hacking or phishing...

Docker setup??? (to skip mongodb)